# Enable live AoN Elasticsearch enrichment (off by default for CORS safety in deployed builds).
# When disabled, card generation still works and cards receive AoN search links instead.
# VITE_ENABLE_AON_LIVE_SEARCH=true
