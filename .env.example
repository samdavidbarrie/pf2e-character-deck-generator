# CORS proxy URL for AoN Elasticsearch requests.
# Required for deployed builds (e.g. GitHub Pages) where direct calls to
# elasticsearch.aonprd.com are blocked by CORS.
# See workers/aon-proxy.js for a ready-to-deploy Cloudflare Worker.
# VITE_AON_PROXY_URL=https://aon-proxy.<you>.workers.dev
